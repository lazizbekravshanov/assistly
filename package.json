{
  "name": "assistly-social-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "start:api": "node src/server.js",
    "start:worker": "node src/worker.js",
    "migrate:postgres": "node scripts/migrate_postgres.js",
    "lint": "node --check src/*.js src/**/*.js test/*.js test/**/*.js",
    "security": "node scripts/security_check.js",
    "test": "rm -rf .test-data && DATA_DIR=.test-data node --test --test-concurrency=1"
  },
  "dependencies": {
    "pg": "^8.18.0"
  }
}
